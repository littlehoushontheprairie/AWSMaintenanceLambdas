{
  "name": "update-instance-ports-lambda",
  "version": "1.0.0",
  "description": "AWS lambda updates instance ports given an instanceName.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "lambda",
    "aws",
    "lightsail",
    "ports"
  ],
  "author": "Ryan Housh",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.715.0"
  }
}
